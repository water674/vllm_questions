这些问题聚焦于**服务调度与性能优化**，属于大模型部署上线最后一公里的核心挑战。如果说之前的KV Cache是静态内存管理，那么现在讨论的就是动态请求调度。

我们逐一来拆解：

### 1. 动态 Batching 是什么？
**定义：** 传统的静态Batching是等数据凑够一批再统一推理；而**动态Batching**是**有请求来就先不急着处理，等一个极短的超时时间（比如几百毫秒），把这段时间内到达的请求攒在一起作为一个Batch去推理**。

**类比：**
-   **静态Batching：** 像定点班车，到点才发车，早来的人得等着，晚来一秒的人得等下一班。
-   **动态Batching：** 像网约车拼车，司机稍微等一两分钟，把附近差不多同时叫车的人拉上一起走。

### 2. Continuous Batching 怎么理解？
**这是vLLM、TensorRT-LLM等框架的核心武器，也称Iteration-level Batching（迭代级批处理）。**

-   **传统Batching（回合级）：** 一个Batch必须一起完成Prefill和所有的Decode。如果Batch里有短句和长句，短句生成完了也只能干等着长句慢慢生成，GPU在这个过程中可能闲置（出现气泡）。
-   **Continuous Batching（迭代级）：**
    -   **理念：** 每做一个Decode迭代（生成一个Token），就重新规划一次Batch。
    -   **操作：** 当Batch里的A请求已经生成完了（输出了EOS，即结束符），立刻把它踢出去，并把等待队列里的C请求加进来（做Prefill）。
    -   **结果：** GPU一直在忙，没有气泡，硬件利用率拉满。

### 3. Prefill/Decode 分离有什么作用？
**背景：** Prefill（计算Prompt）和Decode（逐个生成）的计算特性是冲突的：
-   Prefill：计算密集型，喜欢大Batch。
-   Decode：访存密集型，Batch太大虽好但显存放不下Cache。

**分离解耦（SplitFuse / 分体架构）：**
-   **做法：** 用不同的GPU机器分开处理这两个阶段。
    -   **Prefill池：** 专门负责吃下长Prompt，快速生成第一个Token和KV Cache。
    -   **Decode池：** 专门负责拿着Cache慢悠悠地一个一个生成，并通过高速网络（如RoCE，RDMA over Converged Ethernet）从Prefill池接收Cache。
-   **作用：**
    1.  **避免干扰：** 防止一个大Prompt的Prefill卡住所有正在Decode的请求（即不让计算密集任务阻塞访存密集任务）。
    2.  **资源利用率高：** 针对不同阶段的特点分别优化硬件配置（Prefill池可以用计算卡，Decode池可以用带宽大的卡）。

### 4. 为什么合批能提吞吐？
**核心在于分摊开销和提升利用率：**
1.  **计算密度提升：** 矩阵乘法（GEMM）在小矩阵时，GPU计算单元吃不饱；合批后矩阵变大，能更好地利用GPU的Tensor Core，达到接近理论峰值的算力。
2.  **访存开销分摊：** Decode阶段需要从显存搬运权重和KV Cache。如果只做一个请求，搬运权重的耗时占比很高。合批后，**权重被搬运一次，但可以服务于Batch里的多个请求**，相当于把搬运权重的成本摊薄了，显著提高了吞吐（Tokens/s）。

### 5. 合批会伤延迟吗？
**答案是：会。这是一个典型的Trade-off（吞吐与延迟的权衡）。**

-   **伤的是“首字延迟”（TTFT）：** 动态Batching会让第一个到达的请求等一会儿（等待队列填满），这增加了首字的响应时间。
-   **不影响（甚至改善）“生成速度”：** 一旦开始生成，只要显存不爆，Batch里每个请求的生成速度（Token间延迟）基本不变，因为计算是并行的。
-   **如何应对：** 线上服务通常设置**最大Batch Size**和**最大超时时间**。如果延迟要求极高（如实时对话），可能需要牺牲吞吐，用更小的Batch甚至Batch=1。

### 6. 队列里不同长度怎么排？
这是调度策略问题，核心难点在于**Padding（填充）带来的浪费**。

-   **朴素做法（First-Come-First-Served，先来先服务）：** 容易把长句和短句混在一起。短句生成完了，但因为Continuous Batching还好，如果是传统Batching，短句得等长句算完，且为了维持矩阵形状，短句后面要补一堆无意义的占位符（Padding Token），造成计算浪费。
-   **先进策略（Scheduling）：**
    1.  **按长度分桶：** 把长度相近的请求放在一个Batch里，减少Padding浪费。
    2.  **Shortest Job First（短作业优先，SJF）：** 优先处理短的请求，让它们快速退出系统，避免短请求被长请求“饿死”（Starvation），从而降低平均延迟。
    3.  **连续批处理天然优势：** Continuous Batching下，长度不再是问题，因为它随时可以踢出完成的任务加入新任务，长短句混排的影响比传统Batching小得多。

### 7. 线上最怕的调度坑是什么？
根据业界经验（如Triton、vLLM的Issue），最怕的坑是**“活锁”或“饥饿”**。

1.  **长请求饿死（Starvation）：**
    -   **场景：** 如果系统总是优先处理新来的短请求（为了高吞吐），一个长文本翻译请求可能永远得不到调度，一直在队尾排队（因为总是插进来新的短任务）。
    -   **表现：** 长请求超时，用户侧看到转圈然后报错。

2.  **显存泄露与上下文长度炸弹：**
    -   **场景：** 调度器没考虑到KV Cache的显存占用。如果连续批处理中，Batch里的请求都生成了超长序列（比如偷偷传了个32K的Prompt），KV Cache瞬间暴涨，导致其他请求OOM。
    -   **表现：** 整个服务崩掉，所有连接断开。

3.  **优先级反转：**
    -   **场景：** 高优先级请求被低优先级请求的Prefill阶段阻塞（因为GPU正在做大矩阵计算，无法抢占），导致高优先级请求响应变慢。

**总结：**
大模型推理调度本质上是一个**多目标优化问题**：既要延迟低（SLA，服务等级协议），又要吞吐高（Cost，成本），还要公平（Fairness）。Continuous Batching和Prefill/Decode分离是目前解决这些问题的两大主流手段。
