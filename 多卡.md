这些问题聚焦于**分布式推理的性能剖析**，属于大模型生产环境中的高阶诊断能力。理解这些，意味着你不仅能搭系统，还能精准定位系统"慢"在哪。

我们逐一深入：

### 1. TP/PP 推理各自瓶颈
-   **TP（张量并行）：**
    -   **瓶颈：** **通信带宽**。
    -   **原因：** TP 需要在每个算子（如 Attention、MLP）之后都进行通信（All-Reduce）。这种通信是**同步且高频**的，对节点内通信带宽（如 NVLink）要求极高。一旦跨节点（比如通过 PCIe 或 RoCE），带宽跟不上，TP 的效率会急剧下降。
    -   *形象点说：TP 就像一群人一起写一份稿子，每写一行都要互相传阅核对，通信极其频繁。*
-   **PP（流水线并行）：**
    -   **瓶颈：** **算力利用率（存在Bubble）** 和 **显存带宽**。
    -   **原因：** PP 只在各 Stage 的边界传输数据（Activation 和 Grad），通信量远小于 TP。但 PP 存在流水线气泡（Bubble，即设备空闲等待时间），导致总体算力利用率下降。此外，PP 的每一层都需要读取完整的模型权重，因此受显存带宽限制明显。
    -   *形象点说：PP 就像工厂流水线，虽然通信少，但第一道工序做完前，后续工序得等着。*

### 2. KV Cache 在 TP 下怎么存？
**TP 是按头切分（Split Head）的，所以 KV Cache 也是按头切分存储的。**

-   **MHA（多头注意力）场景：** 假设有 2 张卡，总共 32 个注意力头。
    -   GPU 0 存储头 0-15 的 KV Cache。
    -   GPU 1 存储头 16-31 的 KV Cache。
-   **GQA/MQA（分组查询/多查询注意力）场景：** 由于 KV 头数较少，切分时可能会复制（Replicate）或更精细地切分。但在主流实现中，通常还是按头切，只不过每个头负责的 Query 头更多。
-   **存储逻辑：** 每张卡只存自己负责的那几个头的 K 和 V，**不全量存储**。在 Decode 阶段，当前 Token 的 Q 也被切分到各卡，各卡用自己手里的 Q 片段去查自己手里的 K/V 片段，算出局部注意力分数，最后通过 All-Gather 合并成完整的注意力输出。

### 3. 为什么通信会拖慢 Decode？
**核心原因：计算与通信无法完全重叠（Overlap），且通信占比过高。**

-   **Decode 阶段特性：** 计算量小（一个Token的矩阵运算），但通信量不小（需要同步所有头的中间结果）。
-   **关键点：** Decode 的计算时间（微秒级）往往快于通信时间（毫秒级）。当 GPU 算完需要通信时，它只能干等着，直到数据从别的卡传过来才能继续下一步。
-   **结果：** GPU 计算单元经常处于闲置状态，等待数据从显存（带宽瓶颈）或从其他卡（通信瓶颈）到达。这导致 Decode 的延迟主要由最慢的那根"网线"决定。

### 4. All-Reduce 和 All-Gather 常见在哪？
这是分布式训练/推理中最常见的两个集合通信原语。

-   **All-Reduce（全规约）：**
    -   **场景：** **TP 中的 MLP 层**。
    -   **原因：** 在 MLP 层，数据经过列并行和行并行后，需要将各卡的结果相加求和，得到最终输出。All-Reduce 刚好实现"求和后广播给所有人"。
    -   *比如：大家各自算完一部分，All-Reduce 让所有人都拿到完整的和。*
-   **All-Gather（全收集）：**
    -   **场景：** **TP 中的 Attention 层输出** 和 **PP 的 Stage 边界**（PP 通常用 Send/Recv，但某些实现也会用 All-Gather 来聚合结果）。
    -   **原因：** 在 Attention 层，各卡算出了自己头上 Attention 的结果（片段），需要将这些片段按头顺序拼接成完整的注意力输出。All-Gather 负责收集所有卡的数据并拼接。
    -   *比如：大家把自己算的那部分结果贡献出来，拼成完整结果给每个人。*

### 5. 多级更容易抖动的原因
"抖动"指推理延迟忽高忽低，不稳定。多级（如 TP+PP+DP）并行下更容易抖动，原因有三：

1.  **木桶效应（同步屏障）：** 多级并行意味着存在多个同步点（Barrier）。只要有一张卡（因系统中断、内存带宽竞争等）慢了一点，所有卡都得等它。慢卡成了整个集群的拖累。
2.  **网络微突发（Micro-burst）：** 多级并行会产生复杂的通信模式（TP 的内网通信 + PP 的外网通信）。当这些通信模式叠加时，可能造成网络交换机缓存的瞬时溢出，导致丢包重传，引发延迟尖峰。
3.  **显存带宽竞争：** 当 TP 和 PP 共用同一张卡时，计算、通信（通过网卡拷贝显存数据）、以及 KV Cache 读写，会同时争抢显存带宽，导致实际运算速度不稳定。

### 6. 怎么做性能 Profiling？
Profiling 是用工具定量分析"时间都去哪了"。推荐工具和方法：

1.  **工具选择：**
    -   **PyTorch Profiler（TensorBoard）：** 适合开发环境，能看到每个算子的耗时、CUDA Kernel 耗时。
    -   **NVIDIA Nsight Systems（nsys）：** **强烈推荐**。它能展示 CPU 和 GPU 的时间线，清晰看到 GPU 是空闲（Idle）还是在计算（Compute），以及通信（MemCpy 或 Communication）占了多少时间。
    -   **NVIDIA Nsight Compute（ncu）：** 更底层的 Kernel 分析，看算得有多快，有没有触发慢速路径。
2.  **关键分析点：**
    -   **查看 GPU 利用率：** 如果 GPU 利用率低，但显存读写高，说明是**访存瓶颈**。
    -   **查看通信时间占比：** 如果 GPU 大部分时间在等待通信，说明**通信瓶颈**。
    -   **查看 Kernel 计算时间：** 如果 Kernel 本身跑得慢，可能是**算子实现不佳**或**计算瓶颈**。

### 7. 说一个你会抓的关键指标
我会抓 **"Time to First Token (TTFT)"** 和 **"Inter-Token Latency (ITL)"** 的 **分位值（P99）**。

**理由：**
-   **TTFT（首字延迟）：** 直接影响用户体验的"第一印象"。如果 TTFT 高，用户会觉得"卡住了"。
-   **ITL（Token间延迟）：** 反映生成速度。如果 ITL 抖动大（P99 远高于平均值），说明系统不稳定，用户会感觉对话"一顿一顿的"。
-   **为什么抓 P99（99分位）而不是平均值：**
    -   平均值会掩盖问题。可能 99% 的请求都很快（10ms），但 1% 的慢请求（1000ms）会严重影响那部分用户的体验，甚至导致服务超时。抓 P99 能揪出那些"抖动"的尾部延迟，这正是分布式系统中多级并行容易引发的典型问题。

**诊断示例：** 如果观察到 P99 ITL 飙升，我会立刻用 Nsight Systems 抓取慢请求的时间线，大概率会看到在某个 All-Reduce 通信点上，某张卡因为显存带宽竞争或网络拥堵，导致其他卡长时间等待。
